<!DOCTYPE html>
<html lang="en">

<head>
  <title>Aa-So-Sa Asian Fusion - Order Status</title>
  <%- include('partials/_header') %>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/mdbootstrap/4.19.1/css/mdb.min.css" rel="stylesheet">
  <script type="text/javascript" src="/scripts/app.js"></script>
</head>

<body>
  <%- include('partials/_navbar') %>
  <div class="container mt-5 bg-white p-4 text-center">
    <h3>Order #: <%= completedOrder.id %></h3>
    <!-- Order pending (waiting for confirmation from owner) -->
    <% if (completedOrder.order_status === 'pending') { %>
      <h4>Please wait while we confirm your order with the restaurant</h4>
      <div class="spinner-border" role="status">
        <span class="sr-only">Pending...</span>
      </div>
      <p class="mt-4 mb-4"> Order Placed At: <%= completedOrder.placed_at %> </p>
    <% } else if (completedOrder.order_status === 'confirmed') { %>
      <h3>Your order has been confirmed!</h3>
      <h4>Estimated Wait Time: <%= completedOrder.wait_time %> minutes </h4>

      <img src="/images/wok.png" style="width: 8rem; height: auto"  class="animated headShake infinite slow" alt="wok animation">
      <p>Your order is being prepared.</p>
      <p>The estimated pick up time is <%= completedOrder.ready_at %> </p>
      <p> We will notify you via SMS when it is ready!</p>
    <!-- <div class="progress mx-auto" style="width: 70%">
      <div class="progress-bar bg-danger" role="progressbar" style="width: 25%"></div>
    </div> -->
    <% }  else { %>
      <h3>Your order is ready for pick up!</h3>
      <img src="/images/food.png" style="width: 8rem; height: auto"  class="animated rubberBand infinite slow" alt="wok animation">
      <h5 class="mt-3">Thank you for choosing Aa-So-Sa!</h5>
    <% } %>
  </div>

</body>

</html>
