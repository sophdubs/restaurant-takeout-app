<!DOCTYPE html>
<html lang="en">

<head>
  <title>Aa-So-Sa Asian Fusion - Order Status</title>

  <%- include('partials/_header') %>


  <script type="text/javascript" src="/scripts/app.js"></script>


</head>

<body>
  <%- include('partials/_navbar') %>
  <!-- Order pending (waiting for confirmation from owner) -->
  <div class="container mt-5 bg-white p-4 text-center">
    <h3>Order #: <%= completedOrder.id %></h3>
    <h4>Order Placed At: <%= completedOrder.placed_at.toLocaleString("en-US", {timeZone: "Canada/Eastern"}); %> </h4>
    <% if (completedOrder.order_status === 'confirmed') { %>
    <h3>Your order has been confirmed</h3>
    <p>Order is being prepared</p>
    <div class="d-flex justify-content-center mt-5">
    </div>
    <h4>Expected Pickup Time: <%= `${completedOrder.wait_time} minutes` %> </h4>
    <div class="progress mx-auto" style="width: 70%">
      <div class="progress-bar bg-danger" role="progressbar" style="width: 25%"></div>
    </div>
    <% } else if (completedOrder.order_status === 'pending') { %>
      <p>Thank you for choosing Aa-Sa-So!</p>
      <div class="spinner-border" role="status">
        <span class="sr-only">Pending...</span>
      </div>
      <p class="mt-4 mb-4"> Please wait while we confirm your order with the restaurant</p>
    <% } %>
  </div>

</body>

</html>
